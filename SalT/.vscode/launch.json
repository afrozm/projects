{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SalT",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/SalT",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build Debug"
        }
    ]
}
